module.exports = {
  presets: [
    ['@babel/preset-env', {
      targets: {node: 'current'}
    }]
  ],
  {{#if this.js}}
  env: {
    test: {
      presets: [
        'power-assert'
      ]
    }
  },
  {{/if}}
};
