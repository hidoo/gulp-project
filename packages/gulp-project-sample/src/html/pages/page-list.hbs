---
# 管理用 ID・ページ名
uid: Z
title: ページリスト

# SEO関連
seo:
  title:
  keywords:
  description:

# OGP関連
ogp:
  title:
  description:
  type:
  url:
  image:
  appID:

# フラグ
is:
  top: false
---
{{#extend 'base'}}
{{#content 'body'}}

  <h1>ページリスト</h1>

  {{#each (sortArray @root.pages order='ascend' key='uid') }}
    {{#isnt this.uid 'Z'}}

      {{#markdown}}
```
{{{toJson this prettify=true}}}
```
      {{/markdown}}

      <h2>[{{this.uid}}] <a class="mod-text__link" href="{{@root.path.depth}}{{this.path.relative}}">{{this.title}}</a></h2>

      <p>SEOタイトル</p>
      <p style="color:#999;">{{#if this.seo.title}}{{this.seo.title}} <span style="color:#0f0;font-size:12px;">(ページ固有値)</span>{{else}}{{@root.site.seo.title}} <span style="color:#00f;font-size:12px;">(サイト共通値)</span>{{/if}}</p>

      <p>SEOディスクリプション</p>
      <p style="color:#999;">{{#if this.seo.description}}{{this.seo.description}} <span style="color:#0f0;font-size:12px;">(ページ固有値)</span>{{else}}{{@root.site.seo.description}} <span style="color:#00f;font-size:12px;">(サイト共通値)</span>{{/if}}</p>

      <p>SEOキーワード</p>
      <p style="color:#999;">{{#if this.seo.keywords}}{{this.seo.keywords}} <span style="color:#0f0;font-size:12px;">(ページ固有値)</span>{{else}}{{@root.site.seo.keywords}} <span style="color:#00f;font-size:12px;">(サイト共通値)</span>{{/if}}</p>

      <p>OGP用タイトル</p>
      <p style="color:#999;">{{#if this.ogp.title}}{{this.ogp.title}} <span style="color:#0f0;font-size:12px;">(ページ固有値)</span>{{else}}{{@root.site.ogp.title}} <span style="color:#00f;font-size:12px;">(サイト共通値)</span>{{/if}}</p>

      <p>OGPディスクリプション</p>
      <p style="color:#999;">{{#if this.ogp.description}}{{this.ogp.description}} <span style="color:#0f0;font-size:12px;">(ページ固有値)</span>{{else}}{{@root.site.ogp.description}} <span style="color:#00f;font-size:12px;">(サイト共通値)</span>{{/if}}</p>

      <p>OGPタイプ</p>
      <p style="color:#999;">{{#if this.ogp.type}}{{this.ogp.type}} <span style="color:#0f0;font-size:12px;">(ページ固有値)</span>{{else}}{{@root.site.ogp.type}} <span style="color:#00f;font-size:12px;">(サイト共通値)</span>{{/if}}</p>

      <p>OGP用URL</p>
      <p style="color:#999;">{{#if this.ogp.url}}{{this.ogp.url}} <span style="color:#0f0;font-size:12px;">(ページ固有値)</span>{{else}}{{@root.site.ogp.url}} <span style="color:#00f;font-size:12px;">(サイト共通値)</span>{{/if}}</p>

      <p>OGP用画像URL</p>
      <p style="color:#999;">{{#if this.ogp.image}}{{this.ogp.image}} <span style="color:#0f0;font-size:12px;">(ページ固有値)</span>{{else}}{{@root.site.ogp.image}} <span style="color:#00f;font-size:12px;">(サイト共通値)</span>{{/if}}</p>

    {{/isnt}}
  {{/each}}

{{/content}}
{{/extend}}
